{
  "permissions": {
    "defaultMode": "auto"
  },
  "model": "opus",
  "skillOverrides": {},
  "enabledPlugins": {
    "warp@claude-code-warp": true,
    "ponytail@ponytail": true,
    "stripe@claude-plugins-official": false,
    "agent-skills@addy-agent-skills": true,
    "obsidian@obsidian-skills": true,
    "github@claude-plugins-official": true
  },
  "extraKnownMarketplaces": {
    "claude-code-warp": {
      "source": {
        "source": "github",
        "repo": "warpdotdev/claude-code-warp"
      }
    },
    "ponytail": {
      "source": {
        "source": "github",
        "repo": "DietrichGebert/ponytail"
      }
    },
    "addy-agent-skills": {
      "source": {
        "source": "github",
        "repo": "addyosmani/agent-skills"
      }
    },
    "obsidian-skills": {
      "source": {
        "source": "github",
        "repo": "kepano/obsidian-skills"
      }
    }
  },
  "modelSettings": {
    "claude-opus-5": {
      "effortLevel": "high"
    },
    "claude-opus-5-5": {
      "effortLevel": "high"
    }
  },
  "autoMode": {
    "environment": [
      "### Org-wide",
      "**Organization**: None configured",
      "**Cloud provider(s)**: None configured",
      "**Repository visibility**: this config repo (`temirvvs/claude-code-config`) is public, so anything committed or pushed to it is published; assume other repos are private unless a visibility check in the transcript shows public",
      "**Internal sharing / snippet hosting**: None configured — treat public paste/gist services as outside the trust boundary",
      "**Secrets management**: None configured",
      "**Default / protected branches**: `main` for this config repo; unknown for other repos",
      "**CI/CD deploy targets**: None configured",
      "**Network posture**: None configured",
      "**Host containment**: None configured — assume Claude Code runs on an ordinary developer machine with open internet",
      "**Source control**: The trusted repo (this Claude config checkout at ~/.claude, tracked at git@github.com:temirvvs/claude-code-config.git per its CLAUDE.md) — no additional orgs configured",
      "**Trusted internal domains**: None configured",
      "**Trusted cloud buckets**: None configured — bucket config scan did not complete cleanly, no evidence available",
      "**Key internal services**: None configured",
      "**Internal package registry**: None configured",
      "**Sensitive data locations & audiences**: any file or store holding personal data, confidential business data, credentials, regulated data, or similarly sensitive material; preserve exact handles when known and share only with audiences cleared at the [named+specifics] bar",
      "**Data retention / declassification**: None configured",
      "**Sensitive remote targets**: any namespace, host, or container whose name carries `prod` or `production` as a whole word or name segment",
      "**Protected deployment namespaces / environments**: None configured — fall back to the Sensitive remote targets heuristic",
      "**Protected IaC scopes**: IAM, RBAC, networking, quota, and node-pool resources; anything whose name or tag carries `prod` or `production` as a whole word or name segment",
      "### User-specific",
      "**Primary use of Claude Code**: software development, plus personal machine and config maintenance (Homebrew, macOS defaults)",
      "**Trusted repo**: this repo, the `~/.claude` config checkout tracked at `git@github.com:temirvvs/claude-code-config.git` (public); routine actions under `~/.claude/` are treated as routine",
      "**Org-specific CLIs**: None configured — non-standard CLIs seen (brew, osascript, defaults, open, pmset, xmllint) are ordinary macOS/dev-machine tools, not org-specific"
    ]
  }
}
